# Matiks Leaderboard Project Configuration Template
# Copy this file to .env and fill in your values

# [Backend]
# The port the Go server will listen on locally (default: 8080)
PORT=8080

# The public URL of the frontend (used for CORS)
FRONTEND_URL=http://localhost:8081

# [Frontend]
# The public URL of the backend (referenced in Expo code)
EXPO_PUBLIC_API_URL=http://127.0.0.1:8080
